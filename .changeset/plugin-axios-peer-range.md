---
"@kubb/plugin-axios": patch
---

Raise the `axios` peer dependency lower bound from `^1.7.2` to `^1.19.0` to match the version actually tested in CI, reconciling it with the `axios` devDependency range.
